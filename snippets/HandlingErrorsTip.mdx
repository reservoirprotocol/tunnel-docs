export const HandlingErrorsTip = () => (
  <Tip>In some cases execution or quote generation can result in an error. In some cases errors can be standardized and handled accordingly. Learn more about error handling <a href="/references/api/handling-errors">here</a>.</Tip>
)